import { ServerBuild } from '@remix-run/server-runtime';
import { ServerRouteManifest } from '@remix-run/server-runtime/dist/routes.js';

interface DevToolsServerConfig {
    wsPort?: number;
    withWebsocket?: boolean;
    silent?: boolean;
    logs?: {
        cookies?: boolean;
        defer?: boolean;
        actions?: boolean;
        loaders?: boolean;
        cache?: boolean;
        siteClear?: boolean;
    };
}
declare const defineServerConfig: (config: DevToolsServerConfig) => DevToolsServerConfig;

declare const withServerDevTools: <T extends ServerBuild>(build: T, config?: DevToolsServerConfig) => T & {
    routes: {};
};
declare const augmentLoadersAndActions: <T extends ServerRouteManifest>(routes: T) => {};

export { augmentLoadersAndActions, defineServerConfig, withServerDevTools };
