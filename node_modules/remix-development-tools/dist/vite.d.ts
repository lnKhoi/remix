import { Plugin } from 'vite';

interface DevToolsServerConfig {
    wsPort?: number;
    withWebsocket?: boolean;
    silent?: boolean;
    logs?: {
        cookies?: boolean;
        defer?: boolean;
        actions?: boolean;
        loaders?: boolean;
        cache?: boolean;
        siteClear?: boolean;
    };
}

declare const remixDevTools: (args?: {
    pluginDir?: string;
    server?: Omit<DevToolsServerConfig, "wsPort" | "withWebsocket">;
}) => Plugin[];

export { remixDevTools };
